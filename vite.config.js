import { defineConfig } from 'vite'

export default defineConfig({
    // Use relative base path so assets are loaded correctly on GitHub Pages
    // regardless of the repository name.
    base: './',
})
